# Metasploitable2 VM Configuration for AUVAP PPO Remote Execution
# Generated automatically for IP: 192.168.126.128

# VM Connection Details
export RL_VM_HOST="192.168.126.128"
export RL_VM_PORT="22"
export RL_VM_USER="msfadmin"
export RL_VM_PASSWORD="msfadmin"

# Python interpreter on the VM (custom-built Python 3.4.10 lives in /opt/python3.4)
export RL_VM_PYTHON="/opt/python3.4/bin/python3.4"

# SSH configuration for Metasploitable2 (uses legacy algorithms)
export RL_VM_SSH_OPTIONS="-o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedKeyTypes=+ssh-rsa -o StrictHostKeyChecking=no"

# Workspace directory on VM
export RL_VM_WORKSPACE="/tmp/auvap_rl"

# Connection settings
export RL_VM_TIMEOUT="30"

# ⚠️  SECURITY WARNING ⚠️
# Metasploitable2 is INTENTIONALLY VULNERABLE - keep it isolated!
# - Use host-only or internal network adapter
# - Never expose to internet or production networks
# - Only use in controlled testing environments

echo "✓ Metasploitable2 VM configuration loaded"
echo "  VM: $RL_VM_HOST"
echo "  User: $RL_VM_USER"
echo "  Python: $RL_VM_PYTHON (version 3.4.10)"
